# 酒局管家 JIUJU 用户 Web 环境变量示例
# 复制为 .env 后填写真实值。

# API 基础地址。
# 开发环境走 Vite 代理：/api/v1 或 /api 都会被转发到后端 :3000/api/v1。
# 生产环境建议留空（默认 /api/v1），由 nginx / 反向代理把 /api 转发到后端；
# 也可显式指定完整地址，如 https://api.example.com/api/v1
VITE_API_BASE_URL=/api/v1
